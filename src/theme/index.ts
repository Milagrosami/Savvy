

const { width, height } = Dimensions.get("window");
import { Dimensions } from "react-native";
const spacing = {
  xs: 8,
  s: 16,
  m: 24,
  l: 32,
  xl: 40,
};

const radius = {
  xs: 8,
  s: 10,
  m: 15,
  l: 25,
  xl: 50,
};

const iconSize = {
  s: 14,
  m: 20,
  l: 24,
  xl: 28,
};


export { height, iconSize, radius, spacing, width };